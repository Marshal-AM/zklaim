/** @deprecated Use activityLog.ts — kept for existing imports. */
export {
  createActivityLogger as createSubmitClaimLogger,
  formatActivityData as formatSubmitClaimData,
  type ActivityLogEntry as SubmitClaimLogEntry,
  type ActivityLogLevel as SubmitClaimLogLevel,
  type ActivityLogger as SubmitClaimLogger,
} from "./activityLog";
